export type Note = {
    id: string;
    title: string;
    editors: string[];
    userId: string;
    createdAt: string;
    updatedAt: string;
}

export type SidebarNote = {
    id: string;
    title: string;
}